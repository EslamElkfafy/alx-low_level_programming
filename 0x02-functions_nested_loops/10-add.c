#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers
 *
 * @n1: the first number
 * @n2: the secound number
 * Return: the result
 */
int add(int n1, int n2)
{
	int result;

	result = n1 + n2;
	return (result);
}
